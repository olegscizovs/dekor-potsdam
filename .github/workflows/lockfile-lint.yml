name: Lockfile Lint

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint-lockfile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.1.0

      - name: Install dependencies
        run: pnpm install --ignore-scripts --frozen-lockfile || true

      - name: Run lockfile-lint
        run: npx lockfile-lint --path pnpm-lock.yaml --type pnpm --validate-https --allowed-hosts npm